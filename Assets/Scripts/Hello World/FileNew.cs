// 네임스페이스 선언부
using UnityEngine; //현재 (FileNew)클래스 파일에서 UnityEngine 네임스페이스를 사용한다


// 클래스 선언부 : 클래스의 이름은 FileNew
public class FileNew : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    //Start Method 선언   : 프로그램을 시작할때 1번만 실행
    void Start()
    {
        // 명령문: 콘솔창에 출력하는 명령문
        Debug.Log("Hello World");
    }

    // Update is called once per frame
    
    //Update Method 선언  : 프로그램 시작 후에 매 프레임마다 실행
    void Update()
    {
        
    }
}
