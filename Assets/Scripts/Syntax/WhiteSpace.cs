using UnityEngine;

//WhiteSpace: 공백은 프로그램 무시
public class WhiteSpace : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("C#");
        Debug.  Log  (    "C#"    );
        Debug
            .Log(
                "C#");
    }
}
